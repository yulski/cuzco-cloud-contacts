<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}{% endblock %}</title>
    {% block styles %}{% endblock %}
</head>
<body>
    <header id="page-header">
        <nav>
            <ul>
                {% if (user is defined) and (user is not null) %}
                    <li>
                        <a href="{{ path(PATH_DASHBOARD) }}">Dashboard</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_USER_CONTACTS) }}">Contact list</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_CREATE_CONTACT) }}">Create contact</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_PROFILE) }}">View your profile</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_LOGOUT) }}">Log out</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path(PATH_LANDING_PAGE) }}">Home</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_LOGIN) }}">Log in</a>
                    </li>
                    <li>
                        <a href="{{ path(PATH_REGISTRATION) }}">Register</a>
                    </li>
                {% endif %}
            </ul>
        </nav>
        <h1>{% block pageHeading %}{% endblock %}</h1>
    </header>
    <div id="message">
        {% if flashMessage is defined %}
            {{ flashMessage }}
        {% endif %}
    </div>
    <div id="main">
        {% block main %}{% endblock %}
    </div>
    <footer id="footer">
        Some footer stuff idk. &copy; 2017 Yulski
    </footer>
    {% block scripts %}{% endblock %}
</body>
</html>